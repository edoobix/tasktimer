{{ form_start(form) }}
<div>
    <div class="row">{{ form_row(form.login) }}</div>
    <div class="row">{{ form_row(form.roles) }}</div>
    <div class="row">{{ form_row(form.password) }}</div>
    <button type="submit" class="btn btn-primary">{{ button|default('Сохранить') }}</button>
</div>
{{ form_end(form) }}
