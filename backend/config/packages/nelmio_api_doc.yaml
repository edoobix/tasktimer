nelmio_api_doc:
    documentation:
        info:
            title: tasktimer
            description: App for administrating tasks!
            version: 1.0.0
        # --- ДОБАВЛЯЕМ ЭТОТ БЛОК ---
        components:
            securitySchemes:
                bearerAuth:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - bearerAuth: []
        # ---------------------------
    areas:
        path_patterns:
            - ^/api(?!/doc$)
